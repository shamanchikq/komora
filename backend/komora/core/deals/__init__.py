"""Deals: price memory, the deal scan and the deals screen (Plan 4 Tasks 2–3).

Everything here is computed by Python from reads — no model request. A deal is
`oldPrice > price` on a product id (D2); a «звичайна ціна» is a median of enough shelf
snapshots at one branch (D3); coupons stay text.
"""
