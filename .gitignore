# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/

# Environment / secrets
.env
.env.*
!.env.example

# Databases
*.db
*.db-journal
# Backups taken before a destructive migration or a wipe. `*.db` does not cover
# `verify.db.bak-20260812`, and these hold encrypted OAuth tokens.
*.db.bak*
*.bak-*
*.sqlite
*.sqlite3

# Node / web
node_modules/
web/dist/

# Editors & OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local scratch — captured MCP payloads before sanitising.
# tests/fixtures/mcp/*.json IS committed, but only after scrubbing PII.
scratch/
*.local.json

# Local artefacts from scripts/verify_mcp.py
backend/verify.db
